# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=lovelace
DB_PASSWORD=lovelace
DB_NAME=lovelace

# JWT
JWT_SECRET=changeme
JWT_EXPIRES_IN=3600s

# SMTP (configure for real email sending)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
MAIL_FROM="Lovelace <no-reply@lovelace.edu>"
